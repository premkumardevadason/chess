apiVersion: v1
kind: Service
metadata:
  name: {{ include "chess-app.fullname" . }}
  labels:
    {{- include "chess-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chess-app.selectorLabels" . | nindent 4 }}